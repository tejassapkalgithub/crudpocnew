package com.example.demo.controller;

public class EmployeeController {
	
	
	/*
	 * @PostMapping("/authenticate") public String generateToken(@ResponseBody
	 * AuthRequest authrequest) throws Exception { try {
	 * authenticationManager.authenticate(new
	 * UsernamePasswordAuthenticationToken(authrequest.getUsername(),
	 * authrequest.getPassword()));
	 * 
	 * } catch (Exception e) { // TODO: handle exception throw new
	 * Exception("Invalid username and password "); } return
	 * jwtUtil.generateToken(authrequest.getUserName()); }
	 */


}
